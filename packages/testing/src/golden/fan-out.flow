flow "fan-out" {
  node a: prompt @(0, 0) {
    template = "a-prompt"
    providerId = "mock"
    modelId = "m"
  }

  node b: prompt @(0, 1) {
    template = "b-prompt"
    providerId = "mock"
    modelId = "m"
  }
}
